function [g] = limX(g)
%--------------------------------------------------------------------------
% dry limiter:
%g          = dryLimiter(g);

    g = wetDryCellAverage(g);
%--------------------------------------------------------------------------
end